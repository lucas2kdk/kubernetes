# Tenant base — apps shared across all clusters. Currently empty: hello-world
# was retired (its image runs as root on :80 and couldn't meet the restricted
# PodSecurity profile), and excalidraw/gitlab are prod-only (pulled directly by
# the prod-fsn overlay). Kept as the seam for future fleet-wide tenants.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources: []
