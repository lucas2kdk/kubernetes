apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# No tenants onboarded yet. Add tenant bases (namespace, RBAC, ServiceAccount,
# ESO SecretStore, Flux source scoped to the tenant repo) here as they appear.
resources: []
