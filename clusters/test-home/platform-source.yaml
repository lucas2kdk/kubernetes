apiVersion: source.toolkit.fluxcd.io/v1
kind: OCIRepository
metadata:
  name: platform
  namespace: flux-system
spec:
  interval: 10m
  url: oci://oci.rosenvold.tech/platform
  ref:
    # ponytail: this is THE version pin — bump it in a PR to promote test-home
    tag: "1.4.0"
