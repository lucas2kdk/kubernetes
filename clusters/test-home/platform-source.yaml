apiVersion: source.toolkit.fluxcd.io/v1
kind: OCIRepository
metadata:
  name: platform
  namespace: flux-system
spec:
  interval: 10m
  url: oci://ghcr.io/lucas2kdk/platform
  ref:
    # ponytail: this is THE version pin — bump it in a PR to promote test-home
    tag: "1.3.0"
