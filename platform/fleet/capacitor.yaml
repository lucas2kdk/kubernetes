# Capacitor — Flux GitOps dashboard. A reconcile root (no dependsOn).
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: capacitor
  namespace: flux-system
spec:
  path: ./base/capacitor
