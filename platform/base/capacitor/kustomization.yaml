# Capacitor base: just wires in the OCIRepository + nested Kustomization that
# pull the Flux dashboard UI manifests (see capacitor.yaml).
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - capacitor.yaml
