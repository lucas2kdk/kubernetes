# PDB for the ESO controller. Keeps voluntary disruptions (node drains) from
# taking all replicas at once, but stays drain-safe on a single-node cluster.
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: external-secrets
  namespace: external-secrets
spec:
  # ponytail: maxUnavailable — same reasoning as kyverno PDB; minAvailable: 1 at 1 replica
  # deadlocks drain.
  maxUnavailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: external-secrets
      app.kubernetes.io/instance: external-secrets
