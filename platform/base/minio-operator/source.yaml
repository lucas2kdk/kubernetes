# MinIO operator chart repo. Centralized in flux-system with the other platform
# sources.
apiVersion: source.toolkit.fluxcd.io/v1
kind: HelmRepository
metadata:
  name: minio-operator
  namespace: flux-system
spec:
  interval: 1h
  url: https://operator.min.io
