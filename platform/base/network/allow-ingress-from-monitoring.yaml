apiVersion: cilium.io/v2
kind: CiliumClusterwideNetworkPolicy
metadata:
  name: allow-ingress-from-monitoring
spec:
  description: >-
    Allow ingress to workload namespaces from the Prometheus stack in the
    monitoring namespace so scrape jobs reach their targets. Without this the
    default-deny policy blocks all monitoring traffic to workload pods even
    though monitoring itself is excluded from default-deny (it's a platform
    namespace). Mirrors the endpoint selector and namespace exclusion list of
    allow-ingress-from-traefik so both policies apply to the same set of pods.
  # NOTE: this NotIn list mirrors the four guarded exclusion lists but is NOT
  # itself checked by scripts/check-namespace-lists.sh. Keep it in sync by hand
  # when you add a platform namespace, or monitoring will lose scrape access to
  # the new namespace's workloads.
  endpointSelector:
    matchExpressions:
      - key: k8s:io.kubernetes.pod.namespace
        operator: NotIn
        values:
          - kube-system
          - kube-node-lease
          - kube-public
          - flux-system
          - kyverno
          - policy-reporter
          - cert-manager
          - external-secrets
          - tailscale
          - traefik
          - monitoring
          - netdata
          - vector
          - headlamp
          - homepage
          - tsidp
          - trivy-system
          - longhorn-system
          - cilium-gateway # Cilium Gateway API trial; mirrors the guarded lists.
  ingress:
    - fromEndpoints:
        - matchLabels:
            k8s:io.kubernetes.pod.namespace: monitoring
