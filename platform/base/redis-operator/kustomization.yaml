# Redis operator base (controller + CRDs). Redis instance CRs live in the
# tenants that need them, not here.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - source.yaml
  - release.yaml
